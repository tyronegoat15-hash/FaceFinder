{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix face-finder-v2/backend"
  },
  "deploy": {
    "startCommand": "node face-finder-v2/backend/server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  }
}
